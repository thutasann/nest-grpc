# Nestjs gRPC Microservices

This is the mini microservices with `nest.js` and `gRPC`.

## Script

### Generate App

```bash
nest generate app auth
```

### Run Service

```bash
yarn start:dev auth
```
